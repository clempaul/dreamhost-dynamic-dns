# Install using systemd

This script `systemd_ddns_setup.sh` creates the user `dreamhostdns` and sets up the
systemd.service and systemd.timer to run at boot and every 12 hours.

It assumes you are putting executables in /usr/local/bin
